CREATE TABLE  "TEACHERS" 
   (	"T_ID" NUMBER NOT NULL ENABLE, 
	"USERNAME" VARCHAR2(20) NOT NULL ENABLE, 
	"FULL_NAME" VARCHAR2(25) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(40), 
	"CONTACT_NO" VARCHAR2(25), 
	"GENDER" NUMBER(1,0), 
	"STATUS" NUMBER(1,0), 
	"DATE_OF_JOINING" DATE NOT NULL ENABLE, 
	"FATHER_NAME" VARCHAR2(25), 
	"PERMANENT_ADDRESS" VARCHAR2(60) NOT NULL ENABLE, 
	"DATE_OF_BIRTH" DATE, 
	"YEARS_OF_EXPERIENCE" NUMBER, 
	"CNIC" NUMBER, 
	"PASSWORD" VARCHAR2(16) NOT NULL ENABLE, 
	"IMAGE" BLOB NOT NULL ENABLE, 
	"BASIC_INCOME" NUMBER, 
	"DESIGNATION" VARCHAR2(50), 
	"INCOME_TAX" NUMBER, 
	"GROUP_INSURANCE" NUMBER, 
	"FAMILY_BENEFIT_FUND" NUMBER, 
	"LOAN" NUMBER, 
	"DEDUCTION" NUMBER, 
	"SECTION_NAME" VARCHAR2(15), 
	 CONSTRAINT "TEACHERS_PK" PRIMARY KEY ("T_ID", "USERNAME") ENABLE
   ) ;ALTER TABLE  "TEACHERS" ADD CONSTRAINT "TEACHERS_SECTION_NAME_FK" FOREIGN KEY ("SECTION_NAME")
	  REFERENCES  "SECTIONS" ("SECTION_NAME") ENABLE;
/

alter table "TEACHERS" add constraint  "TEACHERS_PK" primary key ("T_ID","USERNAME")
/



CREATE TABLE  "STUDENTS" 
   (	"S_ID" NUMBER NOT NULL ENABLE, 
	"USERNAME" VARCHAR2(20) NOT NULL ENABLE, 
	"FULL_NAME" VARCHAR2(25) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(40), 
	"CONTACT_NO" VARCHAR2(25), 
	"GENDER" NUMBER(1,0), 
	"STATUS" NUMBER(1,0), 
	"ADMISSION_DATE" DATE, 
	"ADMISSION_NO" NUMBER, 
	"FATHER_NAME" VARCHAR2(25), 
	"MOTHER_NAME" VARCHAR2(25), 
	"DATE_OF_BIRTH" DATE, 
	"NATIONALITY" VARCHAR2(15), 
	"RELIGION" VARCHAR2(15), 
	"CLASS_NAME" VARCHAR2(5), 
	"SECTION_NAME" VARCHAR2(10), 
	"S_ROLL_NO" VARCHAR2(15), 
	"PASSWORD" VARCHAR2(16) NOT NULL ENABLE, 
	"GUARDIAN_FULL_NAME" VARCHAR2(25) NOT NULL ENABLE, 
	"GUARDIAN_CONTACT_NO" VARCHAR2(25) NOT NULL ENABLE, 
	"GUARDIAN_ADDRESS" VARCHAR2(40) NOT NULL ENABLE, 
	"PRE_SCHOOL" NUMBER(1,0), 
	"PRIMARY_SCHOOL" NUMBER(1,0), 
	"MIDDLE" NUMBER(1,0), 
	"MIDDLE_YEAROFPASS" VARCHAR2(10), 
	"MIDDLE_INSTITUTION" VARCHAR2(50), 
	"HIGH_SCHOOL_SSC" NUMBER(1,0), 
	"SSC_YEAROFPASS" VARCHAR2(10), 
	"SSC_INSTITUTION" VARCHAR2(50), 
	"HIGHER_SECONDARY_HSSC" NUMBER(1,0), 
	"HSSC_YEAROFPASS" VARCHAR2(10), 
	"HSSC_INSTITUTION" VARCHAR2(50), 
	"IMAGE" BLOB, 
	"PRE_INSTITUTION" VARCHAR2(50), 
	"PRIMARY_INSTITUTION" VARCHAR2(50), 
	 CONSTRAINT "STUDENTS_PK" PRIMARY KEY ("S_ID", "USERNAME") ENABLE
   ) ;ALTER TABLE  "STUDENTS" ADD CONSTRAINT "STUDENTS_SECTION_NAME_FK" FOREIGN KEY ("SECTION_NAME")
	  REFERENCES  "SECTIONS" ("SECTION_NAME") ENABLE;




CREATE table "COURSES" (
    "C_ID"              NUMBER NOT NULL,
    "COURSE_CODE"       VARCHAR2(15),
    "C_DISCRIPTION"     VARCHAR2(100),
    "T_ID"              NUMBER,
    "AFFECTED_SECTIONS" VARCHAR2(50),
    "DATE_OF_ADDITION"  DATE,
    constraint  "COURSES_PK" primary key ("C_ID")
)
/




CREATE TABLE  "MARKS" 
   (	"MAX_MARKS" NUMBER, 
	"MIN_MARKS" NUMBER, 
	"OBTAINED_MARKS" NUMBER, 
	"S_ID" NUMBER, 
	"C_ID" NUMBER, 
	"USERNAME" VARCHAR2(20)
   ) ;ALTER TABLE  "MARKS" ADD CONSTRAINT "FK_SEARCH" FOREIGN KEY ("S_ID", "USERNAME")
	  REFERENCES  "STUDENTS" ("S_ID", "USERNAME") ENABLE;ALTER TABLE  "MARKS" ADD CONSTRAINT "MARKS_C_ID_FK" FOREIGN KEY ("C_ID")
	  REFERENCES  "COURSES" ("C_ID") ENABLE;


CREATE TABLE  "SECTIONS" 
   (	"SECTION_NAME" VARCHAR2(15) NOT NULL ENABLE, 
	"ASSIGNED_ROOM_NO" NUMBER NOT NULL ENABLE, 
	"T_ID" NUMBER, 
	"AFFECTED_CLASSES" VARCHAR2(50), 
	"DATE_OF_ADDITION" DATE, 
	 CONSTRAINT "SETIONS_PK" PRIMARY KEY ("SECTION_NAME") ENABLE
   ) ;


CREATE TABLE  "FEE" 
   (	"S_ID" NUMBER, 
	"S_USERNAME" VARCHAR2(20), 
	"TUTION_FEE" NUMBER, 
	"LAB_CHARGES" NUMBER, 
	"SECURITY_CHARGES" NUMBER, 
	"HEALTH_SERVICES" NUMBER, 
	"TOTAL_FEE" NUMBER, 
	"AVALIBLE_CONCESSION" NUMBER, 
	"FINAL_FEE" NUMBER, 
	"DUE_DATE" DATE, 
	"FINE" NUMBER
   ) ;ALTER TABLE  "FEE" ADD CONSTRAINT "FK_SEARCH2" FOREIGN KEY ("S_ID", "S_USERNAME")
	  REFERENCES  "STUDENTS" ("S_ID", "USERNAME") ENABLE;


CREATE TABLE  "ATTENDANCE" 
   (	"T_ID" NUMBER, 
	"C_ID" NUMBER, 
	"T_USERNAME" VARCHAR2(20), 
	"S_ROLL_NO" NUMBER NOT NULL ENABLE, 
	"ATTENDANCE" NUMBER(1,0) NOT NULL ENABLE, 
	"SECTION_NAME" VARCHAR2(15), 
	"DATE_OF_ATTENDANCE" DATE
   ) ;ALTER TABLE  "ATTENDANCE" ADD CONSTRAINT "ATTENDANCE_C_ID_FK" FOREIGN KEY ("C_ID")
	  REFERENCES  "COURSES" ("C_ID") ENABLE;ALTER TABLE  "ATTENDANCE" ADD CONSTRAINT "ATTENDANCE_SECTION_NAME_FK" FOREIGN KEY ("SECTION_NAME")
	  REFERENCES  "SECTIONS" ("SECTION_NAME") ENABLE;ALTER TABLE  "ATTENDANCE" ADD CONSTRAINT "FK_SEARCH3" FOREIGN KEY ("T_ID", "T_USERNAME")
	  REFERENCES  "TEACHERS" ("T_ID", "USERNAME") ENABLE;

CREATE TABLE  "ADMINS" 
   (	"A_ID" NUMBER NOT NULL ENABLE, 
	"USERNAME" VARCHAR2(20) NOT NULL ENABLE, 
	"A_PASSWORD" VARCHAR2(16) NOT NULL ENABLE, 
	"FULL_NAME" VARCHAR2(20), 
	"EMAIL" VARCHAR2(50), 
	"CONTACT_NO" VARCHAR2(20), 
	"GENDER" NUMBER(1,0), 
	"STATUS" NUMBER(1,0), 
	"REGISTRATION_DATE" DATE, 
	"IMAGE" BLOB, 
	 CONSTRAINT "ADMINS_PK" PRIMARY KEY ("A_ID") ENABLE
   ) ;


CREATE TABLE  "SALARY" 
   (	"USR_ID" NUMBER, 
	"USERNAME" VARCHAR2(20), 
	"SALARY_AMOUNT" NUMBER, 
	"PAYMENT_DATE" DATE, 
	"MODE_OF_PAYMENT" VARCHAR2(20), 
	"PAYMENT_MODE_DETAILS" VARCHAR2(100), 
	"DEDUCTION_AMOUNT" NUMBER, 
	"TOTAL_PAY" NUMBER
   ) ;ALTER TABLE  "SALARY" ADD CONSTRAINT "FK_SEARCH1" FOREIGN KEY ("USR_ID", "USERNAME")
	  REFERENCES  "TEACHERS" ("T_ID", "USERNAME") ENABLE;


================================================================
Foreign Keys                                                   |
================================================================
alter table students add constraint students_section_name_FK foreign key(section_name) references sections(section_name);
alter table teachers add constraint teachers_section_name_FK foreign key(section_name) references sections(section_name);
alter table marks add constraint FK_search foreign key(s_id, username) references students(s_id, username);
alter table marks add constraint marks_c_id_FK foreign key(c_id) references courses(c_id);
alter table salary add constraint FK_search1 foreign key(usr_id, username) references teachers(t_id, username);
alter table fee add constraint FK_search2 foreign key(s_id, s_username) references students(s_id, username);
alter table attendance add constraint FK_search3 foreign key(t_id, t_username) references teachers(t_id, username);
alter table attendance add constraint attendance_c_id_FK foreign key(c_id) references courses(c_id);
alter table attendance add constraint attendance_section_name_FK foreign key(section_name) references sections(section_name);